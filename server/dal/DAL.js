require('dotenv').config()

class DAL {
    
    /* static async connect() {
        try {
            await connect(process.env.MONGO_URL);
        } catch(e) {
            throw e
        }
    }
    static async disconnect() {
        try {
            await disconnect()
        } catch(e) {
            throw e
        }
    } */
}

module.exports = {DAL}